name: fce
description: A new Flutter project.
publish_to: 'none' # Rimuovi questa riga se vuoi pubblicare su pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  qr_flutter: ^4.1.0   # ✅ Aggiunto il package per il QR code

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/biglietto.jpg

